Extract ATS keywords from this job description.
Return ONLY valid JSON with keys:
must_have_skills, tools_technologies, responsibilities, domain_terms, soft_skills, certifications,
suggested_title, summary_keywords.

Rules:
- Preserve exact phrases from the JD where possible.
- Deduplicate lists; max 30 per list.
