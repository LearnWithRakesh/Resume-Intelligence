Using the structured ResumeProfile JSON and (optional) JD Analysis JSON:
Generate an ATS-friendly resume draft.

Return ONLY JSON with keys:
headline, summary, skills, experience, projects, keywords_to_add, risks_or_gaps

Rules:
- Never invent new employers, dates, tools, or metrics.
- Use placeholders for missing metrics and add them to risks_or_gaps.
- Prefer JD keyword alignment where truthful.
